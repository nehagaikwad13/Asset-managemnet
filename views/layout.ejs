<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title || 'AssetFlow' %></title>
  <script src="https://cdn.tailwindcss.com?plugins=forms,container-queries"></script>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
  <style>
    body { font-family: 'Inter', sans-serif; }
  </style>
  <% if (typeof printStyles !== 'undefined' && printStyles) { %>
  <style>
    @media print {
      nav, .print-hidden, .btn-section { display: none !important; }
      table { width: 100%; }
    }
  </style>
  <% } %>
</head>
<body class="bg-gray-100 text-gray-800">
  <div class="min-h-screen flex">
    <% if (typeof showSidebar === 'undefined' || showSidebar) { %>
    <aside class="w-64 bg-blue-800 text-white flex flex-col p-6 space-y-6 shadow-lg">
      <h1 class="text-2xl font-bold mb-4">AssetFlow</h1>
      <nav class="flex-1 space-y-4 text-sm font-medium">
        <a href="/" class="block py-2 px-4 rounded hover:bg-blue-700">Dashboard</a>
        <a href="/add-product" class="block py-2 px-4 rounded hover:bg-blue-700">Add Product</a>
        <a href="/assign-product" class="block py-2 px-4 rounded hover:bg-blue-700">Assign Product</a>
        <a href="/view-database" class="block py-2 px-4 rounded hover:bg-blue-700">View Database</a>
        <a href="/return-product" class="block py-2 px-4 rounded hover:bg-blue-700">Return Product</a>
        <a href="/reports" class="block py-2 px-4 rounded hover:bg-blue-700">Reports</a>
      </nav>
      <a href="/login" class="mt-auto text-sm py-2 px-4 rounded bg-red-600 text-center hover:bg-red-700">Logout</a>
    </aside>
    <% } %>
    
    <main class="<%= showSidebar === false ? 'w-full' : 'flex-1' %> p-6">
      <%- body %>
    </main>
  </div>
</body>
</html>